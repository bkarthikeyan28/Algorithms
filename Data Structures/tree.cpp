#include<iostream>
using namespace std;
struct node
{
	int key;
	node *child[100];
};
struct node* insert(struct node *node,int x)
{
	node->key=x;


}
int main()
{
	struct node *root=NULL;
	cin>>root->key;
	cin>>n;
	for(int i=0;i<n;i++)
		root=insert(root,x);
}